<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Remory" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">My Site</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <% if user_signed_in?%>
            <%= content_tag(:li, link_to("Sign out", destroy_user_session_path, method: "delete"))%>
          <% else %>
            <li><a href="#" data-reveal-id="sign-up-form">Sign up</a></li>
            <li><a href="#" data-reveal-id="sign-in-form">Sign in</a></li>
          <%end%>
        </ul>

      </section>
    </nav>
    <div id="sign-up-form" class="reveal-modal" data-reveal>
      <!-- <h2>Awesome. I have it.</h2> -->
       <%= render "devise/registrations/new" %>
      <a class="close-reveal-modal">&#215;</a>
    </div>
    <div id="sign-in-form" class="reveal-modal" data-reveal>
      <!-- <h2>Awesome. I have it.</h2> -->
       <%= render "devise/sessions/new" %>
      <a class="close-reveal-modal">&#215;</a>
    </div>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <%= javascript_include_tag "application" %>
    <%= yield :javascript %>
  </body>
</html>
